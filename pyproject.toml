[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "customer-segmentation"
version = "0.1.0"
description = "E-commerce customer segmentation project"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
